<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.pastoturistico">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".RegistroActivity" />
        <activity android:name=".SplashActivity">

        </activity>
        <activity android:name=".PerfilActivity" />
        <activity
            android:name=".HotelesActivity"
            android:label="@string/title_activity_hoteles"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RestaurantesActivity"
            android:label="@string/title_activity_restaurantes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BaresActivity"
            android:label="@string/title_activity_bares"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListaActivity" />
        <activity
            android:name=".BarDrawerActivity"
            android:label="@string/title_activity_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HotelDrawerActivity"
            android:label="@string/title_activity_hotel_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RestauranteDrawerActivity"
            android:label="@string/title_activity_restaurante_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PerfilDrawerActivity"
            android:label="@string/title_activity_perfil_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainDrawerActivity"
            android:label="@string/title_activity_main_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ExtraDrawerActivity"
            android:label="@string/title_activity_extra_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HistoriaExtraDraweActivity"
            android:label="@string/title_activity_historia_extra_drawe"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ArtesaniaExtraDrawerActivity"
            android:label="@string/title_activity_artesania_extra_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GastronomiaExtraDrawerActivity"
            android:label="@string/title_activity_gastronomia_extra_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">

        </activity>
    </application>

</manifest>